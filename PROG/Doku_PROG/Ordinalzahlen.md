Bei einer Enumeration (Definierung eines Typs) bekommen die Werte der Reihe nach eine Ordinalzahl

```pascal
Type
	Boolean = (False, True); //hier gilt also False < True, weil 0 , 1
	
	TrafficLight = (red, orange, green); // Ordinalzahlen: 0, 1, 2


```

siehe Scalartypes.pas