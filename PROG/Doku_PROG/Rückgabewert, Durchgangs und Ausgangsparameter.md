```pascal
procedure()
```

Rückgabewert: "Ergebnis" einer Funktion
Durchgangsparameter: wird einer Prozedur (manchmal auch Funktion) per Referenz übergeben und wird dann verändert wieder weitergegeben.
Ausgangsparameter: Eine Variable wird per Referenz an eine Prozedur übergeben und dort wird ein neuer Wert zugeordnet.

alle feld elemente größer als bestimmte zahl in ein neues feld speichern

``

```pascal
procedure FilterExtreme(a: IntArray; extremes: IntArray; threshold, n, nExtremes: integer) //n: Anzahl der belegten Elementen
```

