---
Arbeitszeit: 4.77
Rückentraining: false
Schlafzeit: 9.45
HandyScreenOn: 0
---
## Important notes

#### EIR:
- Decoder, Encoder, Mux lesen --> dann Aufgabe machen
- evtl. EIR von vorne anfangen, durchzuarbeiten
#### BWL:
- Aufgaben anschauen
#### STP:
 - Strategie überlegen

## Tasks
```tasks
not done
```


[[_🚀 Dashboard]]


```timekeep
{"entries":[{"name":"EIR","startTime":"2025-12-31T08:21:13.383Z","endTime":"2025-12-31T10:18:50.990Z","subEntries":null},{"name":"EIR","startTime":null,"endTime":null,"subEntries":[{"name":"Part 1","startTime":"2025-12-31T10:51:02.000Z","endTime":"2025-12-31T12:04:53.000Z","subEntries":null},{"name":"Part 2","startTime":"2025-12-31T12:35:40.342Z","endTime":"2025-12-31T12:47:04.527Z","subEntries":null},{"name":"Part 3","startTime":"2025-12-31T13:40:36.006Z","endTime":"2025-12-31T13:52:44.827Z","subEntries":null},{"name":"Part 4","startTime":"2025-12-31T14:27:34.571Z","endTime":"2025-12-31T15:09:43.272Z","subEntries":null},{"name":"Part 5","startTime":"2025-12-31T15:20:25.605Z","endTime":"2025-12-31T15:49:40.237Z","subEntries":null}]}]}
```


## Timekeep Sync Skript
```dataviewjs
const file = Array.isArray(dv.current().file.path) ? dv.current().file.path[0] : dv.current().file.path;
const tFile = app.vault.getAbstractFileByPath(file);

// 1. Gesamte Datei einlesen
const content = await app.vault.read(tFile);

// 2. Timekeep Block suchen & parsen
const timekeepMatch = content.match(/```timekeep\s+([\s\S]*?)\s+```/);

if (timekeepMatch) {
    const data = JSON.parse(timekeepMatch[1]);
    let totalMs = 0;

    // Logik für Entries und SubEntries
    data.entries.forEach(e => {
        if (e.startTime && e.endTime) totalMs += (new Date(e.endTime) - new Date(e.startTime));
        if (e.subEntries) {
            e.subEntries.forEach(s => {
                if (s.startTime && s.endTime) totalMs += (new Date(s.endTime) - new Date(s.startTime));
            });
        }
    });

    const totalHours = parseFloat((totalMs / 3600000).toFixed(2));

    // 3. Frontmatter aktualisieren, falls der Wert abweicht
    await app.fileManager.processFrontMatter(tFile, (frontmatter) => {
        if (frontmatter["Arbeitszeit"] !== totalHours) {
            frontmatter["Arbeitszeit"] = totalHours;
            console.log(`Sync: Arbeitszeit auf ${totalHours}h aktualisiert.`);
        }
    });

    dv.paragraph(`✅ **Timekeep Sync aktiv:** ${totalHours}h übertragen.`);
}
```