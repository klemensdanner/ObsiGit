Interface: rein abstrakte Klasse - ohne Datenkomponenten und ohne Implementierungen der Methoden - nur abstrakte Methoden

Object o = null;

(o instance of String) ist dann false, weil ein null sicherlich kein String ist

eine ClassCastExeption ist immer ein Programmierfehler. wenn man eine Typumwandlung verwendet, muss man das immer prüfen ob die möglich sein wird